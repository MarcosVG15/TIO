SQLAlchemy>=2.0.36,<3.0
psycopg[binary]>=3.2.3
pgvector>=0.3.6
python-dotenv>=1.0.1
alembic>=1.14.0
pydantic[email]>=2.9.0

openai>=1.60.0

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
# Required for multipart/form-data - avatar uploads are the only endpoint that
# takes a file, and FastAPI raises at import time without it.
python-multipart>=0.0.20
# Used directly by flights.py. Already present transitively via openai, but a
# direct import deserves a direct declaration.
httpx>=0.28.0
google-auth[requests]>=2.36.0
PyJWT>=2.10.0
argon2-cffi>=23.1.0
